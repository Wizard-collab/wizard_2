from wizard.gui.video_manager import video_manager_widget
'''
import os
import shutil
video = os.path.abspath("videos/0.mp4")
count = 0
for a in range(1,100):
    new_vid = os.path.join(os.path.dirname(video), f"{a}.mp4")
    shutil.copyfile(video, new_vid)
'''
